# Session Gateway Bridge
USE_EXTERNAL_NODES=true
PORT=3001

# Comma-separated browser origins allowed in production (required when NODE_ENV=production).
# Example: CORS_ORIGINS=https://app.example.com,https://www.example.com
# Dev defaults to http://localhost:5173 and :5174 when unset.
# CORS_ORIGINS=
